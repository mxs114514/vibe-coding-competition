# AI 智能菜谱推荐系统 —— 数据库设计文档

> 版本：1.0  
> 最后更新：2026年1月  
> 设计目标：支持用户食材管理、AI 菜谱生成、收藏评分、购物清单、智能推荐等核心功能  
> 登录方式：手机号（无邮箱）  
> 枚举值统一采用 **数字编码（TINYINT）**，提升性能与可维护性

---

## 📌 全局编码对照表

### 食材分类 (`category`)

| 编码 | 含义   |
|------|--------|
| 1    | 蔬菜   |
| 2    | 肉类   |
| 3    | 海鲜   |
| 4    | 主食   |
| 5    | 调味料 |

### 用户食材状态 (`status`)

| 编码 | 含义   |
|------|--------|
| 1    | 已有   |
| 2    | 缺少   |

### 菜系 (`cuisine`)

| 编码 | 含义   |
|------|--------|
| 1    | 中餐   |
| 2    | 西餐   |
| 3    | 日韩   |
| 4    | 东南亚 |

### 基础味型 (`taste_base`)

| 编码 | 含义   |
|------|--------|
| 1    | 甜     |
| 2    | 咸     |
| 3    | 酸     |
| 4    | 鲜     |
| 5    | 清淡   |
| 6    | 复合   |

### 辣度等级 (`spice_level`)

| 编码 | 含义   |
|------|--------|
| 0    | 不辣   |
| 1    | 微辣   |
| 2    | 中辣   |
| 3    | 重辣   |
| 4    | 爆辣   |

### 菜谱难度 (`difficulty`)

| 编码 | 含义   |
|------|--------|
| 1    | 简单   |
| 2    | 中等   |
| 3    | 复杂   |

---

## 🗂️ 表结构详解

### 1. `user` —— 用户表

存储用户基本信息，用于身份认证。

| 字段名       | 类型         | 说明 |
|--------------|--------------|------|
| `id`         | BIGINT       | 用户唯一ID（主键） |
| `username`   | VARCHAR(50)  | 昵称，全局唯一 |
| `phone`      | VARCHAR(20)  | 手机号，用于登录，全局唯一 |
| `password`   | VARCHAR(255) | 加密后的密码（建议 BCrypt） |
| `created_at` | TIMESTAMP    | 注册时间，自动记录 |

---

### 2. `ingredient` —— 标准食材库

维护结构化食材数据，含营养与过敏信息。

| 字段名                | 类型            | 说明 |
|-----------------------|------------------|------|
| `id`                  | BIGINT          | 食材ID（主键） |
| `name`                | VARCHAR(100)    | 食材名称（如“鸡胸肉”） |
| `category`            | TINYINT         | 分类编码（见对照表） |
| `unit`                | VARCHAR(20)     | 默认单位（如“g”、“个”），默认“份” |
| `calories_per_100g`   | DECIMAL(8,2)    | 每100克热量（kcal） |
| `protein_per_100g`    | DECIMAL(8,2)    | 每100克蛋白质（g） |
| `carbs_per_100g`      | DECIMAL(8,2)    | 每100克碳水（g） |
| `fat_per_100g`        | DECIMAL(8,2)    | 每100克脂肪（g） |
| `allergens`           | TEXT            | 过敏原列表，逗号分隔（如“坚果,乳制品”） |
| `created_at`          | TIMESTAMP       | 数据录入时间 |

---

### 3. `user_ingredient` —— 用户食材记录

记录用户拥有的或计划购买的食材。

| 字段名         | 类型       | 说明 |
|----------------|------------|------|
| `id`           | BIGINT     | 记录ID（主键） |
| `user_id`      | BIGINT     | 关联用户ID |
| `ingredient_id`| BIGINT     | 关联标准食材ID |
| `amount`       | DECIMAL(10,2)| 数量（如 200.5） |
| `unit`         | VARCHAR(20)| 单位（可覆盖默认） |
| `status`       | TINYINT    | 状态：1=已有，2=缺少 |

---

### 4. `recipe` —— 菜谱表（核心）

存储所有菜谱信息，支持 AI 生成与人工录入。

| 字段名               | 类型         | 说明 |
|----------------------|--------------|------|
| `id`                 | BIGINT       | 菜谱ID（主键） |
| `name`               | VARCHAR(100) | 菜名 |
| `cuisine`            | TINYINT      | 菜系编码 |
| `taste_base`         | TINYINT      | 基础味型编码 |
| `spice_level`        | TINYINT      | 辣度等级（0=不辣） |
| `cooking_time_minutes`| INT         | 烹饪耗时（分钟） |
| `difficulty`         | TINYINT      | 难度编码 |
| `ingredients_json`   | TEXT         | 所需食材（JSON 格式） |
| `steps_text`         | TEXT         | 烹饪步骤（支持 Markdown） |
| `nutrition_analysis` | JSON         | 营养分析结果（JSON 对象） |
| `ai_generated`       | BOOLEAN      | 是否由 AI 生成（true/false） |
| `created_at`         | TIMESTAMP    | 创建时间 |

> 💡 示例 `ingredients_json`：
>
